//Linear Search
public static void LinearSearch(int[] arr, int searchkey, out int searchKeyIndex)
{
    searchKeyIndex = -1;
    for(int i = 0; i < arr.Length; i++)
    {
        if(searchkey == arr[i])
        {
            searchKeyIndex = i;
            break;
        }
    }
}
